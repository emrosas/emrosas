---

---
<nav>
    <div class="container">
        <a href="/">
            <img src="./logo.svg" alt="My name (Erick Mireles)">
        </a>
        <ul class="navlinks">
            <li><a href="/"><span>1.</span>About</a><div class="underline"/></li>
            <li><a href="#commercial"><span>2.</span>Commercial</a><div class="underline"/></li>
            <li><a href="/"><span>3.</span>Projects</a><div class="underline"/></li>
            <li><a href="/"><span>4.</span>Contact</a><div class="underline"/></li>
        </ul>
    </div>
</nav>
<style>
    nav {
        background-color: hsla(var(--dark-1), 0.8);
        border-bottom: 1px solid hsl(var(--light-2));
        backdrop-filter: blur(6px);
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
    }
    .container {
        padding-top: 1rem;
        padding-bottom: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .navlinks {
        display: flex;
        gap: 2rem;
        align-items: center;
    }
    li {
        list-style: none;
        font-family: var(--font-mono);
        font-weight: 300;
        position: relative;
    }
    li:hover a {
        color: hsl(var(--light-1));
    }
    a {
        text-decoration: none;
        color: hsl(var(--light-2));
        font-size: 1.2rem;
        transition: color 0.2s ease-in-out;
        display: flex;
        gap: 0.25rem;
    }
    span {
        color: hsl(var(--brand-1));
    }
    .underline {
        width: 100%;
        height: 0px;
        background-color: hsl(var(--light-1));
        transition: height 0.2s ease-in-out;
        position: absolute;
        bottom: -0.25rem;
    }
    li:hover .underline {
        height: 1px;
    }
</style>